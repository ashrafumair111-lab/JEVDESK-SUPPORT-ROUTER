# Copy this file to ".env" and replace the placeholder with your real key.
# Get a key at: https://vercel.com -> AI Gateway -> API Keys
VERCEL_AI_GATEWAY_KEY=your_key_here

# Optional: the port the Express server listens on (defaults to 3000).
# PORT=3000
